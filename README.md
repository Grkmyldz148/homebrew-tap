# Homebrew Tap — Görkem YILDIZ

```sh
brew install --cask Grkmyldz148/tap/clean-browser
```

| Cask | Description |
| --- | --- |
| [`clean-browser`](Casks/clean-browser.rb) | Chromium-based macOS browser for clean, framed website screenshots |

> Clean Browser is unsigned (ad-hoc). The cask removes the quarantine flag on install so it opens without the "damaged" prompt.
